// Multiplicaão de 2 números

function multiplicador (n1: number, n2: number): number {
    return n1 * n2;
}
const resultado = multiplicador(2, 3);
console.log(resultado);

//DIZENDO OLÁ

function dizOi(nome: string): string {
    return `Olá, ${nome}`;
    }
    
    const saluto = dizOi("Paulo" );
    console.log(saluto);